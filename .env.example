NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Clave de cifrado en capa de aplicación (AES-256-GCM) para campos sensibles
# del expediente de alumno (contacto de tutor, datos médicos) — ver
# CLAUDE.md 4.4 y src/lib/cifrado/. SOLO de servidor, nunca NEXT_PUBLIC_*.
# 32 bytes en base64. Genera la tuya con:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
CIFRADO_CLAVE=
